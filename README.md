# Gamegos NoSql Library

Store your data in NoSql (key-value) databases.

## Installation

### Install via Composer

Run the following command in the root directory of your project:
```sh
composer require gamegos/nosql:dev-master
```
